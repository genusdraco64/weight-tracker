# Copy to ~/.config/weight-tracker/api.env and chmod 600.
# Required only when binding the REST API beyond localhost.
# Generate a token, for example:
#   python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
WEIGHT_TRACKER_API_TOKEN=replace-with-a-random-token-at-least-32-characters-long
